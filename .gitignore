node_modules/
.dev.vars
test/worker_tmp.mjs

# Credential/token files, never commit
**/freebuff_credentials.json
# Docker container credentials directory, never commit
credentials/
__pycache__/
test/

# Deploy script contains credential reading logic, local use only, do not push
/credentials.yaml
/deploy.sh

# Update records, local use only, do not push to GitHub
CHANGELOG.md

# Wrangler local cache/state, do not commit
.wrangler/

# Vercel local cache/project files, do not commit
.vercel/

# ⚠️ wrangler.toml contains sensitive variables (FREEBUFF_TOKEN, etc.), never commit to GitHub
wrangler.toml

# ⚠️ .env and all files with real variable values, never commit
.env

# Local log analysis files, never commit
logs/

# Hermes local plans, experimental patches, and session data, local only
.hermes/

wrangler.jsonc
*.har
